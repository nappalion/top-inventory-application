<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <a href="/">Back</a>
    <h1>Update item</h1>
    <form action="/item/update/redirect" method="POST" enctype="multipart/form-data">
      <input type="hidden" id="id" name="id" value="<%= item.id %>" required/>
      <label for="name">Name</label>
      <input type="text" id="name" name="name" value="<%= item.name %>" required/>
      <label for="quantity">Quantity</label>
      <input type="number" id="quantity" name="quantity" value="<%= item.quantity %>" required/>
      <input type="hidden" id="old_image_data" name="old_image_data" value="<%= item.image_data %>" />
      <label for="image_data">Photo</label>
      <input type="file" id="image_data" name="image_data" accept="image/*"/>

      <datalist id="categories">
        <% for( let index = 0; index < categories.length; index++ ) { %>
          <option value="<%= categories[index].id %>"><%= categories[index].name %></option>
        <% } %>
      </datalist>

      <label for="category_id">Category</label>
      <input list="categories" id="category_id" type="number" name="category_id" value="<%= item.category_id %>"/>
      <button type="submit">Update item</button>
    </form>
  </body>
</html>
